name: Add Triage member

on:
  issues:
    types: [opened]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Adds user as contribtor
      run: |
        echo $GITHUB_REPOSITORY
        echo $GITHUB_ACTOR
